// ==UserScript==
// @name         PDA Test Script
// @namespace    http://tampermonkey.net/
// @version      1.1
// @description  Test for PDA execution
// @match        https://www.torn.com/factions.php*
// @grant        none
// ==/UserScript==

(function () {
    'use strict';

    // LOG TO CONSOLE
    console.log('PDA SCRIPT: This should show up in the console.');

    // ALERT FOR VISUAL PROOF
    alert('Torn PDA test script is running!');

    // CHANGE PAGE TO SEE IF SCRIPT HAS ACCESS
    document.body.style.background = "#2228a0";

    // Add an obvious element to the page
    const d = document.createElement("div");
    d.textContent = "PDA Script Loaded!";
    d.style.position = "fixed";
    d.style.top = "50px";
    d.style.left = "50%";
    d.style.transform = "translateX(-50%)";
    d.style.background = "#00e676";
    d.style.padding = "20px";
    d.style.zIndex = 9999;
    d.style.fontSize = "2em";
    document.body.appendChild(d);
})();
